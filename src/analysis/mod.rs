/*
 * Analysis cannot take in app context at all.
 * it must either collect all data before the function. or create a monad to
 * instruct a stateful entity to finalize the analysis.
 *
 *
 */
mod color;
